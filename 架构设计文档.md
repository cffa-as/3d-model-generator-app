# 3D模型AI生成平台 - 架构设计文档

## 1. 项目概述

### 1.1 项目简介
基于AI技术的3D模型生成和管理平台，支持文本生成3D、图片生成3D、多图片生成3D等多种生成方式，集成模型历史管理、智能缓存、社区分享、质量评估等功能。面向3D设计师、游戏开发者、创意爱好者等用户群体，通过AI技术降低3D建模门槛。

### 1.2 技术栈
- **前端**: Next.js 14 + TypeScript + Tailwind CSS + React Three Fiber
- **后端**: FastAPI + Python + aiomysql + uvicorn
- **数据库**: MySQL 8.0
- **AI工具模型**: text2vec (文本语义相似度) + trimesh (几何分析)
- **3D渲染**: Three.js + React Three Fiber + GLTFLoader + OrbitControls
- **第三方服务**: Meshy AI API (3D模型生成) + DeepSeek API (AI提示词生成)

### 1.3 核心功能特点
- **智能缓存**: 基于深度学习的语义相似度匹配
- **多模态生成**: 文本、单图、多图三种生成方式
- **AI提示词助手**: DeepSeek驱动的智能提示词生成与优化
- **任务管理**: 全生命周期任务追踪、状态同步、历史管理
- **角色权限控制**: 三级权限体系（未登录/普通用户/管理员）
- **模型精细化**: 预览模型高质量重建、贴图生成、参数优化
- **多格式下载**: GLB/OBJ/FBX/USDZ模型文件 + 贴图文件下载
- **实时3D预览**: 支持旋转、缩放、平移、背景切换、全屏模式
- **社区功能**: 作品分享、评论点赞、设计师排行榜、标签分类
- **管理面板**: 系统统计、用户行为分析、可视化图表
- **质量评估**: 系统自动评估（拓扑结构、几何精准度、渲染效率） + 用户主观评分
- **数据导出**: CSV格式评估报告、统计数据导出
- **等待优化**: 3072小游戏娱乐化等待体验

## 2. 系统架构

### 2.1 前端架构 (`front/`)
```
app/                           # 页面路由 (Next.js App Router)
├── page.tsx                  # 首页 - 产品介绍
├── auth/page.tsx             # 认证页面
├── dashboard/page.tsx        # 工作台 - 生成界面
├── tasks/[page.tsx + [id]/page.tsx] # 任务管理
├── showcase/[page.tsx + [id]/page.tsx] # 模型广场
└── admin/[page.tsx + evaluation/page.tsx] # 管理功能

components/                    # 组件库 (40+组件)
├── auth/ (3个)               # 认证组件
├── tasks/ (12个)             # 任务相关组件  
├── showcase/ (3个)           # 社区功能组件
├── admin/ (6个)              # 管理功能组件
├── layout/ (2个)             # 布局组件
└── ui/ (20+个)               # 基础UI组件

lib/                          # 服务层
├── api.ts                    # API统一封装
├── auth.ts                   # 认证服务
└── tasks.ts                  # 任务服务
```

### 2.2 后端架构 (`backend/`)
```
routes/                       # API路由层 (4个模块)
├── users.py (4个接口)        # 用户认证管理
├── tasks.py (11个接口)       # 任务全流程管理 + AI提示词功能
├── showcase.py (12个接口)    # 社区功能管理
└── admin.py (6个接口)        # 系统管理功能

services/                     # 业务服务层 (6个服务)
├── db.py                     # 数据库服务 + 事务管理
├── auth.py                   # JWT认证服务
├── cache_service.py          # 智能缓存服务
├── storage.py                # 文件存储服务
├── meshy_client.py           # Meshy AI客户端 (3D模型生成)
└── deepseek_client.py        # DeepSeek AI客户端 (提示词生成)

models/                       # 数据模型层
├── user.py                   # 用户数据模型
└── task.py                   # 任务数据模型
```

### 2.3 数据库架构 (`MySQL 8.0`)
```
核心数据表 (6张表):
├── users                     # 用户基础信息
├── generation_tasks          # 任务核心表 (包含评分、参数、状态)
├── model_evaluations         # 详细评估数据
├── model_showcase            # 社区作品展示
├── model_likes              # 点赞关系表
└── model_comments           # 评论系统表

统计视图 (1个视图):
└── designer_stats           # 设计师排行榜数据
```

## 3. 系统功能架构

### 3.1 用户角色功能分层

#### 3.1.1 未登录用户功能
- **首页展示模块**: 产品介绍、精品模型展示、用户吸引和转化
- **认证模块**: 用户注册、登录入口

#### 3.1.2 普通用户功能 
- **工作台模块**: 核心生成功能
  - 文生3D模型 (基础模式 + 高级模式 + AI提示词助手)
  - 图生3D模型 (基础模式 + 高级模式) 
  - 多图生3D模型 (基础模式 + 高级模式)
  - 相似度检查和推荐 (文生和图生)
  - AI提示词生成与优化 (简单描述→详细提示词)
- **任务管理模块**: 
  - 任务历史页面 (所有生成记录)
  - 任务详情页面 (下载、3D预览、用户评分)
- **社区功能模块**:
  - 模型广场 (作品分享、浏览发现)
  - 社区互动 (点赞、评论、收藏)
  - 设计师排行榜 (情绪价值提供)

#### 3.1.3 管理员功能 (普通用户功能 + 额外管理功能)
- **管理面板模块**: 
  - 系统统计数据和可视化
  - 用户行为分析
  - 任务分布统计
- **模型评估模块**:
  - 双重评估体系 (用户评估 + 系统评估)
  - 系统评估三指标 (拓扑结构、几何准确度、渲染效率)
  - 模型列表管理和批量评估
  - 评估详情展示和CSV导出

### 3.2 核心功能模块详细设计

#### 3.2.1 AI提示词助手模块
```
AI提示词功能设计:
├── 智能生成功能
│   ├── 用户输入简单描述 (如："一只猫"、"机器人")
│   ├── DeepSeek API处理 (语言模型理解和扩展)
│   ├── 生成详细提示词 (80-120字，包含形状、颜色、材质、风格等)
│   └── 多种提示词类型 (3D模型、材质纹理、艺术风格)
├── 提示词优化功能
│   ├── 现有提示词输入
│   ├── AI智能优化处理 (精简冗余、增强关键特征)
│   ├── 保持逻辑层次清晰 (80-120字范围)
│   └── 直接输出优化结果
```

#### 3.2.2 工作台生成模块
```
生成模式设计:
├── 基础模式 (面向普通用户)
│   ├── 简化参数 (只需输入核心内容)
│   ├── 预设配置 (系统自动选择常见参数)
│   └── 快速上手 (降低使用门槛)
└── 高级模式 (面向专业用户)  
    ├── 详细参数控制 (AI模型、种子值、面数等)
    ├── 精细化选项 (PBR材质、拓扑优化等)
    └── 专业定制 (满足专业需求)

相似度推荐系统:
├── 文生模型相似度检查 (text2vec语义匹配)
├── 图生模型相似度检查 (imagehash视觉匹配)  
├── 推荐展示 (展示相似已生成模型)
└── 用户选择 (使用推荐 or 生成新模型)
```

#### 3.2.3 任务管理模块
```
任务历史功能:
├── 任务列表展示 (分页、筛选、排序)
├── 任务状态管理 (pending/completed/failed)
├── 任务搜索和筛选 (按类型、状态、时间)
└── 任务删除和清理

任务详情功能:
├── 3D模型预览 (旋转、缩放、平移、背景切换)
├── 文件下载中心 (GLB/OBJ/FBX/USDZ + 贴图文件)
├── 用户评分系统 (1-10分 + 评论)
├── 模型精细化 (预览→贴图)
```

#### 3.2.4 社区功能模块  
```
模型广场:
├── 作品展示系统 (分类、标签、搜索)
├── 内容发现 (热门标签、排序筛选)
├── 作品详情页 (3D预览、作者信息、互动)
└── 个人空间 (我的作品、我的收藏)

社区互动:
├── 点赞系统 (防重复点赞、可以取消赞)
├── 评论系统 (互动、实时更新)
├── 收藏功能 (个人收藏列表管理)
└── 设计师排行榜 (作品数、点赞数排名)
```

#### 3.2.5 管理功能模块
```
管理面板:
├── 系统统计 (用户数、任务数、类型分布)
├── 数据可视化 (饼图、柱状图、趋势图)
├── 用户行为分析 (活跃度、使用习惯)
└── 系统监控 (性能指标、错误统计)

模型评估系统:
├── 双重评估体系
│   ├── 用户主观评估 (1-10分评分 + 评论反馈)
│   └── 系统客观评估 (trimesh几何分析)
├── 三维度系统评估
│   ├── 拓扑结构评估 (35%权重)
│   ├── 几何准确度评估 (35%权重)  
│   └── 渲染效率评估 (30%权重)
├── 评估管理功能
│   ├── 模型列表管理 (筛选、搜索、批量操作)
│   ├── 一键评估 (批量自动评估)
│   ├── 评估详情展示 (详细指标和日志)
│   └── 数据导出 (CSV格式评估报告)
```

### 3.3 用户体验设计考虑
- **门槛分层**: 基础模式降低使用门槛，高级模式满足专业需求
- **智能推荐**: 相似度检查减少重复生成，提升用户体验
- **AI助手生成提示词**: 简单描述即可生成专业提示词，降低3D建模学习成本
- **提示词优化**: AI智能优化现有提示词，提升模型生成质量
- **情绪价值**: 社区功能和排行榜提供成就感和归属感
- **等待优化**: 3072小游戏让等待时间变成娱乐时间

  ## 4. 开发分工

### 4.1 开发人员
陈飞帆 1人 (独立完成全部开发)